<svg version="1.1"
     width="${width}"
     height="${height}"
     xmlns="http://www.w3.org/2000/svg"
>
    <style>
        .text-foreground {
            fill: ${foreground-color};
        }
        .text-shadow {
            fill: #000000;
            fill-opacity: ${shadow-alpha};
        }
    </style>
    ${content}
</svg>
